import * as React from 'react';
import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    useColorScheme,
    View,
  } from 'react-native';
  
const Stack = createNativeStackNavigator();

const LoginScreen = () => {
  return (
    <View>
        <Text>LoginScrenn</Text>
    </View>
  );
};


export default LoginScreen;